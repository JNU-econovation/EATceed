spring:
  datasource:
    url: jdbc:h2:mem:exceed;MODE=MariaDB
    driver-class-name: org.h2.Driver
    username: exceed
    password: exceed
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true